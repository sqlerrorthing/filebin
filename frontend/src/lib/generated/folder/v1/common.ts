// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: folder/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DateTime } from "../../google/type/datetime";
import { EncryptedBlobs } from "./encryption";

export const protobufPackage = "folder.v1";

export interface FolderId {
  value: string;
}

export interface FileId {
  value: string;
}

/** A token to edit files in the folder */
export interface FolderToken {
  value: string;
}

export interface FolderName {
  value: EncryptedBlobs | undefined;
}

export interface Folder {
  id: FolderId | undefined;
  name: FolderName | undefined;
  createdAt: DateTime | undefined;
  expiredAt?: DateTime | undefined;
}

export interface OwnedFolder {
  folder:
    | Folder
    | undefined;
  /** Token to modify folder's content */
  token: FolderToken | undefined;
}

export interface OwnedFolderRef {
  folderId: FolderId | undefined;
  token: FolderToken | undefined;
}

function createBaseFolderId(): FolderId {
  return { value: "" };
}

export const FolderId: MessageFns<FolderId> = {
  encode(message: FolderId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FolderId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFolderId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FolderId {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: FolderId): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FolderId>, I>>(base?: I): FolderId {
    return FolderId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FolderId>, I>>(object: I): FolderId {
    const message = createBaseFolderId();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseFileId(): FileId {
  return { value: "" };
}

export const FileId: MessageFns<FileId> = {
  encode(message: FileId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileId {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: FileId): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileId>, I>>(base?: I): FileId {
    return FileId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileId>, I>>(object: I): FileId {
    const message = createBaseFileId();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseFolderToken(): FolderToken {
  return { value: "" };
}

export const FolderToken: MessageFns<FolderToken> = {
  encode(message: FolderToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FolderToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFolderToken();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FolderToken {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: FolderToken): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FolderToken>, I>>(base?: I): FolderToken {
    return FolderToken.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FolderToken>, I>>(object: I): FolderToken {
    const message = createBaseFolderToken();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseFolderName(): FolderName {
  return { value: undefined };
}

export const FolderName: MessageFns<FolderName> = {
  encode(message: FolderName, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined) {
      EncryptedBlobs.encode(message.value, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FolderName {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFolderName();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = EncryptedBlobs.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FolderName {
    return { value: isSet(object.value) ? EncryptedBlobs.fromJSON(object.value) : undefined };
  },

  toJSON(message: FolderName): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = EncryptedBlobs.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FolderName>, I>>(base?: I): FolderName {
    return FolderName.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FolderName>, I>>(object: I): FolderName {
    const message = createBaseFolderName();
    message.value = (object.value !== undefined && object.value !== null)
      ? EncryptedBlobs.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseFolder(): Folder {
  return { id: undefined, name: undefined, createdAt: undefined, expiredAt: undefined };
}

export const Folder: MessageFns<Folder> = {
  encode(message: Folder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      FolderId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.name !== undefined) {
      FolderName.encode(message.name, writer.uint32(18).fork()).join();
    }
    if (message.createdAt !== undefined) {
      DateTime.encode(message.createdAt, writer.uint32(26).fork()).join();
    }
    if (message.expiredAt !== undefined) {
      DateTime.encode(message.expiredAt, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Folder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFolder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = FolderId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = FolderName.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.createdAt = DateTime.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expiredAt = DateTime.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Folder {
    return {
      id: isSet(object.id) ? FolderId.fromJSON(object.id) : undefined,
      name: isSet(object.name) ? FolderName.fromJSON(object.name) : undefined,
      createdAt: isSet(object.createdAt)
        ? DateTime.fromJSON(object.createdAt)
        : isSet(object.created_at)
        ? DateTime.fromJSON(object.created_at)
        : undefined,
      expiredAt: isSet(object.expiredAt)
        ? DateTime.fromJSON(object.expiredAt)
        : isSet(object.expired_at)
        ? DateTime.fromJSON(object.expired_at)
        : undefined,
    };
  },

  toJSON(message: Folder): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = FolderId.toJSON(message.id);
    }
    if (message.name !== undefined) {
      obj.name = FolderName.toJSON(message.name);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = DateTime.toJSON(message.createdAt);
    }
    if (message.expiredAt !== undefined) {
      obj.expiredAt = DateTime.toJSON(message.expiredAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Folder>, I>>(base?: I): Folder {
    return Folder.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Folder>, I>>(object: I): Folder {
    const message = createBaseFolder();
    message.id = (object.id !== undefined && object.id !== null) ? FolderId.fromPartial(object.id) : undefined;
    message.name = (object.name !== undefined && object.name !== null)
      ? FolderName.fromPartial(object.name)
      : undefined;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null)
      ? DateTime.fromPartial(object.createdAt)
      : undefined;
    message.expiredAt = (object.expiredAt !== undefined && object.expiredAt !== null)
      ? DateTime.fromPartial(object.expiredAt)
      : undefined;
    return message;
  },
};

function createBaseOwnedFolder(): OwnedFolder {
  return { folder: undefined, token: undefined };
}

export const OwnedFolder: MessageFns<OwnedFolder> = {
  encode(message: OwnedFolder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== undefined) {
      Folder.encode(message.folder, writer.uint32(10).fork()).join();
    }
    if (message.token !== undefined) {
      FolderToken.encode(message.token, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OwnedFolder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOwnedFolder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = Folder.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = FolderToken.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OwnedFolder {
    return {
      folder: isSet(object.folder) ? Folder.fromJSON(object.folder) : undefined,
      token: isSet(object.token) ? FolderToken.fromJSON(object.token) : undefined,
    };
  },

  toJSON(message: OwnedFolder): unknown {
    const obj: any = {};
    if (message.folder !== undefined) {
      obj.folder = Folder.toJSON(message.folder);
    }
    if (message.token !== undefined) {
      obj.token = FolderToken.toJSON(message.token);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OwnedFolder>, I>>(base?: I): OwnedFolder {
    return OwnedFolder.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OwnedFolder>, I>>(object: I): OwnedFolder {
    const message = createBaseOwnedFolder();
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? Folder.fromPartial(object.folder)
      : undefined;
    message.token = (object.token !== undefined && object.token !== null)
      ? FolderToken.fromPartial(object.token)
      : undefined;
    return message;
  },
};

function createBaseOwnedFolderRef(): OwnedFolderRef {
  return { folderId: undefined, token: undefined };
}

export const OwnedFolderRef: MessageFns<OwnedFolderRef> = {
  encode(message: OwnedFolderRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folderId !== undefined) {
      FolderId.encode(message.folderId, writer.uint32(10).fork()).join();
    }
    if (message.token !== undefined) {
      FolderToken.encode(message.token, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OwnedFolderRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOwnedFolderRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folderId = FolderId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = FolderToken.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OwnedFolderRef {
    return {
      folderId: isSet(object.folderId)
        ? FolderId.fromJSON(object.folderId)
        : isSet(object.folder_id)
        ? FolderId.fromJSON(object.folder_id)
        : undefined,
      token: isSet(object.token) ? FolderToken.fromJSON(object.token) : undefined,
    };
  },

  toJSON(message: OwnedFolderRef): unknown {
    const obj: any = {};
    if (message.folderId !== undefined) {
      obj.folderId = FolderId.toJSON(message.folderId);
    }
    if (message.token !== undefined) {
      obj.token = FolderToken.toJSON(message.token);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OwnedFolderRef>, I>>(base?: I): OwnedFolderRef {
    return OwnedFolderRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OwnedFolderRef>, I>>(object: I): OwnedFolderRef {
    const message = createBaseOwnedFolderRef();
    message.folderId = (object.folderId !== undefined && object.folderId !== null)
      ? FolderId.fromPartial(object.folderId)
      : undefined;
    message.token = (object.token !== undefined && object.token !== null)
      ? FolderToken.fromPartial(object.token)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
