// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: folder/v1/files.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Empty } from "../../google/protobuf/empty";
import { FileId, FolderId, OwnedFolderRef } from "./common";
import { EncryptedBlobs, EncryptedVault } from "./encryption";

export const protobufPackage = "folder.v1";

export interface DeleteRequest {
  folder: OwnedFolderRef | undefined;
  fileId: FileId | undefined;
}

export interface UploadFileRequest {
  initiate?: InitiateUpload | undefined;
  chunkData?: Uint8Array | undefined;
}

export interface InitiateUpload {
  /** Encryption meta used to encrypt entire file */
  vault: EncryptedVault | undefined;
  metadata: FileMetadata | undefined;
  folder: OwnedFolderRef | undefined;
}

export interface UploadFileResponse {
  id: FileId | undefined;
}

export interface DownloadRequest {
  folder: FolderId | undefined;
  file: FileId | undefined;
}

export interface Blob {
  part: Uint8Array;
}

export interface ListFilesRequest {
  folder: FolderId | undefined;
}

export interface ListFilesResponse {
  files: FileView[];
}

export interface FileView {
  id: FileId | undefined;
  metadata: FileMetadata | undefined;
  size: number;
}

/**
 * Encrypted json payload
 * Contains only path: string, mime: string and hash (of original file): string
 */
export interface FileMetadata {
  value: EncryptedBlobs | undefined;
}

function createBaseDeleteRequest(): DeleteRequest {
  return { folder: undefined, fileId: undefined };
}

export const DeleteRequest: MessageFns<DeleteRequest> = {
  encode(message: DeleteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== undefined) {
      OwnedFolderRef.encode(message.folder, writer.uint32(10).fork()).join();
    }
    if (message.fileId !== undefined) {
      FileId.encode(message.fileId, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = OwnedFolderRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileId = FileId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRequest {
    return {
      folder: isSet(object.folder) ? OwnedFolderRef.fromJSON(object.folder) : undefined,
      fileId: isSet(object.fileId)
        ? FileId.fromJSON(object.fileId)
        : isSet(object.file_id)
        ? FileId.fromJSON(object.file_id)
        : undefined,
    };
  },

  toJSON(message: DeleteRequest): unknown {
    const obj: any = {};
    if (message.folder !== undefined) {
      obj.folder = OwnedFolderRef.toJSON(message.folder);
    }
    if (message.fileId !== undefined) {
      obj.fileId = FileId.toJSON(message.fileId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRequest>, I>>(base?: I): DeleteRequest {
    return DeleteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRequest>, I>>(object: I): DeleteRequest {
    const message = createBaseDeleteRequest();
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? OwnedFolderRef.fromPartial(object.folder)
      : undefined;
    message.fileId = (object.fileId !== undefined && object.fileId !== null)
      ? FileId.fromPartial(object.fileId)
      : undefined;
    return message;
  },
};

function createBaseUploadFileRequest(): UploadFileRequest {
  return { initiate: undefined, chunkData: undefined };
}

export const UploadFileRequest: MessageFns<UploadFileRequest> = {
  encode(message: UploadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initiate !== undefined) {
      InitiateUpload.encode(message.initiate, writer.uint32(10).fork()).join();
    }
    if (message.chunkData !== undefined) {
      writer.uint32(18).bytes(message.chunkData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.initiate = InitiateUpload.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunkData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFileRequest {
    return {
      initiate: isSet(object.initiate) ? InitiateUpload.fromJSON(object.initiate) : undefined,
      chunkData: isSet(object.chunkData)
        ? bytesFromBase64(object.chunkData)
        : isSet(object.chunk_data)
        ? bytesFromBase64(object.chunk_data)
        : undefined,
    };
  },

  toJSON(message: UploadFileRequest): unknown {
    const obj: any = {};
    if (message.initiate !== undefined) {
      obj.initiate = InitiateUpload.toJSON(message.initiate);
    }
    if (message.chunkData !== undefined) {
      obj.chunkData = base64FromBytes(message.chunkData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFileRequest>, I>>(base?: I): UploadFileRequest {
    return UploadFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFileRequest>, I>>(object: I): UploadFileRequest {
    const message = createBaseUploadFileRequest();
    message.initiate = (object.initiate !== undefined && object.initiate !== null)
      ? InitiateUpload.fromPartial(object.initiate)
      : undefined;
    message.chunkData = object.chunkData ?? undefined;
    return message;
  },
};

function createBaseInitiateUpload(): InitiateUpload {
  return { vault: undefined, metadata: undefined, folder: undefined };
}

export const InitiateUpload: MessageFns<InitiateUpload> = {
  encode(message: InitiateUpload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vault !== undefined) {
      EncryptedVault.encode(message.vault, writer.uint32(26).fork()).join();
    }
    if (message.metadata !== undefined) {
      FileMetadata.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.folder !== undefined) {
      OwnedFolderRef.encode(message.folder, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitiateUpload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitiateUpload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vault = EncryptedVault.decode(reader, reader.uint32());
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = FileMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.folder = OwnedFolderRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitiateUpload {
    return {
      vault: isSet(object.vault) ? EncryptedVault.fromJSON(object.vault) : undefined,
      metadata: isSet(object.metadata) ? FileMetadata.fromJSON(object.metadata) : undefined,
      folder: isSet(object.folder) ? OwnedFolderRef.fromJSON(object.folder) : undefined,
    };
  },

  toJSON(message: InitiateUpload): unknown {
    const obj: any = {};
    if (message.vault !== undefined) {
      obj.vault = EncryptedVault.toJSON(message.vault);
    }
    if (message.metadata !== undefined) {
      obj.metadata = FileMetadata.toJSON(message.metadata);
    }
    if (message.folder !== undefined) {
      obj.folder = OwnedFolderRef.toJSON(message.folder);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitiateUpload>, I>>(base?: I): InitiateUpload {
    return InitiateUpload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitiateUpload>, I>>(object: I): InitiateUpload {
    const message = createBaseInitiateUpload();
    message.vault = (object.vault !== undefined && object.vault !== null)
      ? EncryptedVault.fromPartial(object.vault)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? FileMetadata.fromPartial(object.metadata)
      : undefined;
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? OwnedFolderRef.fromPartial(object.folder)
      : undefined;
    return message;
  },
};

function createBaseUploadFileResponse(): UploadFileResponse {
  return { id: undefined };
}

export const UploadFileResponse: MessageFns<UploadFileResponse> = {
  encode(message: UploadFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      FileId.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = FileId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFileResponse {
    return { id: isSet(object.id) ? FileId.fromJSON(object.id) : undefined };
  },

  toJSON(message: UploadFileResponse): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = FileId.toJSON(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFileResponse>, I>>(base?: I): UploadFileResponse {
    return UploadFileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFileResponse>, I>>(object: I): UploadFileResponse {
    const message = createBaseUploadFileResponse();
    message.id = (object.id !== undefined && object.id !== null) ? FileId.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseDownloadRequest(): DownloadRequest {
  return { folder: undefined, file: undefined };
}

export const DownloadRequest: MessageFns<DownloadRequest> = {
  encode(message: DownloadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== undefined) {
      FolderId.encode(message.folder, writer.uint32(18).fork()).join();
    }
    if (message.file !== undefined) {
      FileId.encode(message.file, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.folder = FolderId.decode(reader, reader.uint32());
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = FileId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadRequest {
    return {
      folder: isSet(object.folder) ? FolderId.fromJSON(object.folder) : undefined,
      file: isSet(object.file) ? FileId.fromJSON(object.file) : undefined,
    };
  },

  toJSON(message: DownloadRequest): unknown {
    const obj: any = {};
    if (message.folder !== undefined) {
      obj.folder = FolderId.toJSON(message.folder);
    }
    if (message.file !== undefined) {
      obj.file = FileId.toJSON(message.file);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadRequest>, I>>(base?: I): DownloadRequest {
    return DownloadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadRequest>, I>>(object: I): DownloadRequest {
    const message = createBaseDownloadRequest();
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? FolderId.fromPartial(object.folder)
      : undefined;
    message.file = (object.file !== undefined && object.file !== null) ? FileId.fromPartial(object.file) : undefined;
    return message;
  },
};

function createBaseBlob(): Blob {
  return { part: new Uint8Array(0) };
}

export const Blob: MessageFns<Blob> = {
  encode(message: Blob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.part.length !== 0) {
      writer.uint32(10).bytes(message.part);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Blob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.part = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Blob {
    return { part: isSet(object.part) ? bytesFromBase64(object.part) : new Uint8Array(0) };
  },

  toJSON(message: Blob): unknown {
    const obj: any = {};
    if (message.part.length !== 0) {
      obj.part = base64FromBytes(message.part);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Blob>, I>>(base?: I): Blob {
    return Blob.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Blob>, I>>(object: I): Blob {
    const message = createBaseBlob();
    message.part = object.part ?? new Uint8Array(0);
    return message;
  },
};

function createBaseListFilesRequest(): ListFilesRequest {
  return { folder: undefined };
}

export const ListFilesRequest: MessageFns<ListFilesRequest> = {
  encode(message: ListFilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.folder !== undefined) {
      FolderId.encode(message.folder, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.folder = FolderId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFilesRequest {
    return { folder: isSet(object.folder) ? FolderId.fromJSON(object.folder) : undefined };
  },

  toJSON(message: ListFilesRequest): unknown {
    const obj: any = {};
    if (message.folder !== undefined) {
      obj.folder = FolderId.toJSON(message.folder);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFilesRequest>, I>>(base?: I): ListFilesRequest {
    return ListFilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFilesRequest>, I>>(object: I): ListFilesRequest {
    const message = createBaseListFilesRequest();
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? FolderId.fromPartial(object.folder)
      : undefined;
    return message;
  },
};

function createBaseListFilesResponse(): ListFilesResponse {
  return { files: [] };
}

export const ListFilesResponse: MessageFns<ListFilesResponse> = {
  encode(message: ListFilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      FileView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(FileView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFilesResponse {
    return { files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => FileView.fromJSON(e)) : [] };
  },

  toJSON(message: ListFilesResponse): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => FileView.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFilesResponse>, I>>(base?: I): ListFilesResponse {
    return ListFilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFilesResponse>, I>>(object: I): ListFilesResponse {
    const message = createBaseListFilesResponse();
    message.files = object.files?.map((e) => FileView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFileView(): FileView {
  return { id: undefined, metadata: undefined, size: 0 };
}

export const FileView: MessageFns<FileView> = {
  encode(message: FileView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      FileId.encode(message.id, writer.uint32(26).fork()).join();
    }
    if (message.metadata !== undefined) {
      FileMetadata.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.size !== 0) {
      writer.uint32(16).sint64(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = FileId.decode(reader, reader.uint32());
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = FileMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = longToNumber(reader.sint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileView {
    return {
      id: isSet(object.id) ? FileId.fromJSON(object.id) : undefined,
      metadata: isSet(object.metadata) ? FileMetadata.fromJSON(object.metadata) : undefined,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
    };
  },

  toJSON(message: FileView): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = FileId.toJSON(message.id);
    }
    if (message.metadata !== undefined) {
      obj.metadata = FileMetadata.toJSON(message.metadata);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileView>, I>>(base?: I): FileView {
    return FileView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileView>, I>>(object: I): FileView {
    const message = createBaseFileView();
    message.id = (object.id !== undefined && object.id !== null) ? FileId.fromPartial(object.id) : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? FileMetadata.fromPartial(object.metadata)
      : undefined;
    message.size = object.size ?? 0;
    return message;
  },
};

function createBaseFileMetadata(): FileMetadata {
  return { value: undefined };
}

export const FileMetadata: MessageFns<FileMetadata> = {
  encode(message: FileMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined) {
      EncryptedBlobs.encode(message.value, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = EncryptedBlobs.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileMetadata {
    return { value: isSet(object.value) ? EncryptedBlobs.fromJSON(object.value) : undefined };
  },

  toJSON(message: FileMetadata): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = EncryptedBlobs.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileMetadata>, I>>(base?: I): FileMetadata {
    return FileMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileMetadata>, I>>(object: I): FileMetadata {
    const message = createBaseFileMetadata();
    message.value = (object.value !== undefined && object.value !== null)
      ? EncryptedBlobs.fromPartial(object.value)
      : undefined;
    return message;
  },
};

export type FilesServiceDefinition = typeof FilesServiceDefinition;
export const FilesServiceDefinition = {
  name: "FilesService",
  fullName: "folder.v1.FilesService",
  methods: {
    listFiles: {
      name: "ListFiles",
      requestType: ListFilesRequest as typeof ListFilesRequest,
      requestStream: false,
      responseType: ListFilesResponse as typeof ListFilesResponse,
      responseStream: false,
      options: {},
    },
    uploadFile: {
      name: "UploadFile",
      requestType: UploadFileRequest as typeof UploadFileRequest,
      requestStream: true,
      responseType: UploadFileResponse as typeof UploadFileResponse,
      responseStream: false,
      options: {},
    },
    download: {
      name: "Download",
      requestType: DownloadRequest as typeof DownloadRequest,
      requestStream: false,
      responseType: Blob as typeof Blob,
      responseStream: true,
      options: {},
    },
    delete: {
      name: "Delete",
      requestType: DeleteRequest as typeof DeleteRequest,
      requestStream: false,
      responseType: Empty as typeof Empty,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
