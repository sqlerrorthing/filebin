syntax = "proto2";

import "google/type/datetime.proto";
import "folder/v1/encryption.proto";

package folder.v1;

message FolderId {
  required string value = 1;
}

message FileId {
  required string value = 1;
}

// A token to edit files in the folder
message FolderToken {
  required string value = 2;
}

message FolderName {
  required EncryptedBlobs value = 1;
}

message Folder {
  required FolderId id = 1;
  required FolderName name = 2;
  required google.type.DateTime created_at = 3;
  optional google.type.DateTime expired_at = 4;
}

message OwnedFolder {
  required Folder folder = 1;
  // Token to modify folder's content
  required FolderToken token = 2;
}

message OwnedFolderRef {
  required FolderId folder_id = 1;
  required FolderToken token = 2;
}
